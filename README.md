
## Prueba técnica

Dentro de `backend` y `servicio-de-pagos` están los README.md correspondientes para la documentación de como instalar y ejecutar dichos servicios.

`API Documentation #reference.postman_collection.json` es el archivo .json que tiene los endpinds y demás para su uso en postman